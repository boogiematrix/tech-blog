<div class="row">
    <div class="col">
{{#each posts}}
    <div class="card">
        <div class="card-title d-flex flex-row justify-space-between">
            <h2>
                <a href="/post/{{id}}">{{title}}</a>
            </h2>
            <div>
                Posted by {{User.username}} on {{format_date createdAt}}
            </div>
            <div class="card-body">{{content}}</div>
        </div>
    </div>
{{/each}}
    </div>
</div>